import CasesDashboard from "@/components/CasesDashboard";

export const metadata = { title: "الحالات | LSR OS", description: "قائمة حالات LSR التشغيلية." };
export default function CasesPage() { return <CasesDashboard />; }
